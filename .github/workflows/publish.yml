name: Publish Factorio Mod

on:
  push:
    tags:
      - '*'
jobs:
  publish-mod:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Validate Mod
        uses: xyzzycgn/factorio-mod-uploader-action@2.0.5
        with:
          action: validate

      - name: Create zip
        uses: xyzzycgn/factorio-mod-uploader-action@2.0.5
        with:
          action: compress

      - name: Upload Mod
        uses: xyzzycgn/factorio-mod-uploader-action@2.0.5
        with:
          action: upload
          factorio-api-key: ${{ secrets.FACTORIO_API_KEY }}